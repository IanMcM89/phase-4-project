import styled from "styled-components";

const Header = styled.header`
  background-color: darkblue;
  border-bottom: 0.75rem solid orangered;
  display: flex;
  justify-content: left;
  align-items: center;
  width: auto;
  height: 10vh;
  margin: 0;
  top: 0;
`;

export default Header;